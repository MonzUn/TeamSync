#pragma once
#include <MUtilityTypes.h>

// TODODB: Add to MEngine namespace and remove MEngine prefix in the names

typedef MUtilityBitmaskID MEngineComponentMask;
#define INVALID_MENGINE_COMPONENT_MASK MUTILITY_INVALID_BITMASK_ID

typedef MUtilityID MEngineEntityID;
#define INVALID_MENGINE_ENTITY_ID -1

typedef MUtilityID MEngineTextureID;
#define INVALID_MENGINE_TEXTURE_ID -1